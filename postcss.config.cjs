// PostCSS configuration removed to allow @astrojs/tailwind integration
// to manage PostCSS/Tailwind automatically. Leaving an empty export
// so tooling doesn't error if it expects the file to exist.
module.exports = {};
