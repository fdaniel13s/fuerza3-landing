---
import propuestas from '../data/propuestas.json'
---

<section id="propuestas" class="container-px py-14 md:py-20">
  <div class="max-w-7xl mx-auto">
    <div class="max-w-3xl">
      <h2 class="text-3xl font-bold text-[var(--neutral-800)]">Lo que lograremos por Tacna Heroica</h2>
      <p class="mt-2 text-[var(--neutral-800)]/75">
        10 propuestas concretas, claras y pensadas para ejecutarse con resultados reales.
      </p>
    </div>

    <div class="mt-6 md:mt-8 grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4 md:gap-5">
      {propuestas.map((p) => (
        <article class="card card-hover h-full" data-id={p.id} aria-labelledby={`p-title-${p.id}`}>
          <div class="flex items-start justify-between gap-3">
            <span class="text-xs font-semibold text-[var(--green-700)] bg-[var(--neutral-50)] px-2 py-1 rounded-md">
              #{p.id}
            </span>
            <span class="text-2xl leading-none text-[var(--green-600)]" aria-hidden>{p.icon}</span>
          </div>

          <h3 id={`p-title-${p.id}`} class="mt-3 text-base md:text-lg font-semibold text-[var(--neutral-800)]">
            {p.title}
          </h3>
          <p class="text-sm text-[var(--neutral-800)]/85 mt-2">{p.brief}</p>

          <details class="mt-3">
            <summary class="cursor-pointer text-sm font-semibold text-[var(--green-700)] hover:underline">
              Ver detalle
            </summary>
            <p class="text-sm text-[var(--neutral-800)]/75 mt-2">{p.details}</p>
          </details>

          <div class="mt-3 h-px bg-[var(--neutral-800)]/10" aria-hidden></div>
          <div class="mt-2 text-xs text-[var(--neutral-800)]/55">
            Propuesta prioritaria para Tacna
          </div>
        </article>
      ))}
    </div>
  </div>
</section>
