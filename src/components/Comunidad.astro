---
---
<section id="comunidad" class="container-px py-16 md:py-24 bg-section">
  <div class="max-w-7xl mx-auto">
    <h2 class="text-2xl font-bold mb-4 text-[var(--neutral-800)]">Comunidad y Testimonios</h2>
    <p class="text-[var(--neutral-800)]/80 mb-6">Simpatizantes apoyando la campaña. Comparte tu foto con el Guante #3.</p>

    <!--TODO: Iterate about a data with phot0s of people -->
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
      {Array.from({length:16}).map((_, i) => (
        <figure class="card text-center" key={i}>
          <div class="w-20 h-20 bg-[var(--neutral-50)] rounded-full flex items-center justify-center text-2xl">👥</div>
          <figcaption class="mt-2 text-sm font-semibold text-[var(--neutral-800)]">Apoyante {i+1}</figcaption>
          <div class="text-xs text-[var(--neutral-800)]/70">Tacna</div>
        </figure>
      ))}
    </div>

    <div class="mt-8">
      <button class="btn btn-primary focus-ring">Sube tu foto con el Guante #3</button>
    </div>
  </div>
</section>
